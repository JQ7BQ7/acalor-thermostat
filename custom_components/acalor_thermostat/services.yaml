shift_center:
  target:
    entity:
      integration: acalor_thermostat
      domain: climate
  fields:
    delta:
      required: true
      example: 0.5
      selector:
        number:
          min: -10
          max: 10
          step: 0.1
          unit_of_measurement: "°C"
          mode: box
